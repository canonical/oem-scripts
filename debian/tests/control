Tests:
 pkg-iot-meta,
 pkg-somerville-meta,
 pkg-stella-meta,
 pkg-sutton-meta
Depends: @, ubuntu-dev-tools, git-buildpackage

# Offline counterparts of the Launchpad tests below. They drive the same code
# paths against debian/tests/lp-mock-server, so they need neither credentials
# nor network access.
Tests:
 get-private-ppa-mock,
 git-url-insteadof-setting-mock,
 lp-api-mock,
 lp-dl-attm-mock
Depends: @, python3

Tests:
 bug-bind,
 get-private-ppa,
 lp-api,
 lp-bug,
 review-merge-proposal,
 run-autopkgtest,
 setup-apt-dir
Depends: @

Tests: pkg-list
Depends: @
Restrictions: allow-stderr

Tests:
 bootstrap-meta,
 mir-bug,
 oem-meta-packages
Restrictions: skippable
Depends: @, openssh-client

Tests:
 git-url-insteadof-setting,
 lp-dl-attm,
 recovery-from-iso.sh
Depends: @, openssh-client
Restrictions: allow-stderr
